package packing.Dynamic;

import packing.Common.*;

public class Range {
    public int index;
    public Space sp;
    public int corner;

    public Range(int index, )



    /*public boolean placed; // Is the position of the block determined?
    public int index;
    public int[] xrange;
    public int[] yrange;
    public int[] zrange;

    public Range(boolean placed, int index, int x1, int x2, int y1, int y2, int z1, int z2) {
        this.placed = placed;
        this.index = index;
        this.xrange = new int[2];
        xrange[0] = x1;
        xrange[1] = x2;
        this.yrange = new int[2];
        yrange[0] = y1;
        yrange[1] = y2;
        this.zrange = new int[2];
        zrange[0] = z1;
        zrange[1] = z2;
    }

    public void modify(int o, int lower, int upper) {
        // o == 1 -> xrange, o == 2 -> yrange, o == 3 -> zrange
        if (o == 1) {
            xrange[0] = lower;
            xrange[1] = upper;
        }
        else if (o == 2) {
            yrange[0] = lower;
            yrange[1] = upper;
        }
        else {
            zrange[0] = lower;
            zrange[1] = upper;
        }
    }*/
}
